<section class="card">
    <span class="badge">Dynamic route</span>
    <h1>Hello, <?= e($name) ?>!</h1>
    <p>This value came from the <code>{name}</code> route parameter and was escaped by Ract's <code>e()</code> helper.</p>
    <p><a href="/">Return home</a></p>
</section>
