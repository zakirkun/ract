<section class="card">
    <span class="badge"><?= e($environment) ?></span>
    <h1><?= e($framework) ?></h1>
    <p>Your MVC application is running. Ract keeps the direct controller-and-view workflow of CodeIgniter while using modern PHP and Composer autoloading.</p>
    <p>Try <a href="/hello/Ract"><code>/hello/Ract</code></a> or inspect the JSON endpoint at <a href="/api/status"><code>/api/status</code></a>.</p>
</section>
